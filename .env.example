# Variables de entorno requeridas para producción

# NextAuth Secret - OBLIGATORIO en producción
# Genera uno nuevo con: openssl rand -base64 32
NEXTAUTH_SECRET=your_secret_here

# URL de la aplicación en producción
# Ejemplo: https://tu-dominio.com
NEXTAUTH_URL=https://your-domain.com

# URL base pública (opcional, pero recomendado)
NEXT_PUBLIC_BASE_URL=https://your-domain.com

# URL base de la API (si usas API externa)
NEXT_PUBLIC_API_BASE_URL=https://your-api.com

# Configuración de desarrollo
NODE_ENV=production